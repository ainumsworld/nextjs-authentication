import { type Metadata } from "next";

export namespace USER_METADATA {
  export const profile: Metadata = {
    title: "Profile",
    description:
      "Manage your account settings, update your profile, and customize your experience.",
  };
}
